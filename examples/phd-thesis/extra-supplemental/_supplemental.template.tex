\documentclass[hidelinks,a4paper,11pt,openany]{book}
\usepackage[left=2.6cm,right=2.6cm,top=3cm,bottom=3cm]{geometry}

% Uncomment for label debugging
%\usepackage[inline]{showlabels}
%\renewcommand{\showlabelfont}{\small\ttfamily\color{gray}}
%\showlabels{ref}

% Uncomment to hide the Index DOI from the footer
%\def\showIndexDoi{\false}

% Set (override) the SUPPLEMENT DATE for this item (default is \today)
%\newcommand{\supplementDate}{\today ~(v2)}
%\newcommand{\supplementDate}{2024-12-30 (v2)}

% Include preamble specific to supplemental material
\input{_supplemental.preamble}

% Load style file for supplements
\input{supplemental.sty}

\begin{document}

	% ***** BEGIN CONTENT *****

	% Format is:
	%   \includesupplement{<supplement_number>}{<supplement_name>}{<doi>}	
	%
	% Ensure that a content file exists in the same directory to be included here.
	% Naming convention is:
	%   content-s<supplement_number>.tex
	% i.e.:
	%   content-s999.tex

	\includesupplement{999}{Example Supplement}{10.9999/0987654321}

	% ***** END CONTENT *****

\end{document}
